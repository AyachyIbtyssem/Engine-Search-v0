A task is a set of Computer program program instruction instruction s that are loaded in RAM memory then the address register s is loaded with the initial address to start running the task It is defined as an execution path through address space where part of a plan will be accomplished after executing the task Processes are often called tasks in embedded system embedded operating systems so task gives a meaning of a real time computing real time loading program code from a disk or another part of memory into the newly allocated space building the process tables and other operating systems structures calculating the initial address then executes the process as one or more tasks Both task and process should be distinguished from Event driven programming event which takes place at a specific time and place and which can be planned for in a computer program In a computer graphical user interface an event can be as simple as a mouse click or keystroke 