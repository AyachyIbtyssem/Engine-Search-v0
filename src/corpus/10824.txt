A Las Vegas algorithm is an algorithm that uses random ness If the algorithm gives a result this result will be the correct one The algorithm gambling gambles with the resource s used It may take a very long times to give back a result or it may not give back a result at all A simple example would be a version of the QuickSort algorithm Quicksort is used to quickly sort things Depending on how this element is selected the sorting can take more time or less Most often a Las Vegas algorithm is used to pick the pivot randomly 