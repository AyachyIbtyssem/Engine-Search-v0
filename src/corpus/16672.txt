 This module implements requirelocal p local getArgs require getArgslocal navbar require navbarlocal function trimAndAddAutomaticNewline	 For compatibility with the original 	 implementation which passed some parameters through to trim	 their whitespace This also triggered the automatic newline behavior 	 	s mw ustring gsub	if mw ustring find then		return n s	else		return s	endendfunction p sidebar	if not args then		args getArgs	end	local root mw html create	local child args child and mw text trim 