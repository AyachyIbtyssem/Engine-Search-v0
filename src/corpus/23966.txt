Start date are easy to read plain text date and time templates that emit microformat dates for events These templates are an alternative to the templates that also emit microformats but require dates to be expressed in a special number oriented syntax Fuzzy dates and times are supported For events where the precise time day or month is not known the user may omit these details Time zones are assumed to be local to the event If time zone information is specified the otherwise optional timezone parameter must be set 