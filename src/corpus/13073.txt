 use strict function initializeRefTools 	if 		return 	 	if 		 Enhanced editing toolbar is on Going to load RefToolbar a or b 		if 			 Dialogs are on Loading b 			 getScript 				mw loader using 					mw loader load 				 			 		 else 			 Dialogs are off Loading a 			mw loader using 				mw loader load 			 		 	 else 		 Enhanced editing toolbar is off Loading RefToolbar 		mw loader load 	 	window refToolbarInstalled true if 