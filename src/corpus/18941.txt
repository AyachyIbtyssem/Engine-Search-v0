A print server or printer server is a computer or device that is connected to one or more Computer printer printers and to a Computer network network It accepts print job s from client client computer s then prints the jobs on the requested printer The term can refer to A computer running any of the printing server s software implementing any of the network printing protocols A embedded system dedicated device that connects one or more printers to a local area network It typically has a single LAN connector such as an RJ socket or a wireless connection cite web and one or more physical ports to provide connections to printers Such dedicated device converts network printing protocols from what was sent by client computer s to what the printers can understand 