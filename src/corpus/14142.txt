 Style for football tableslocal pp function pp header	 Load relevant modules	local yesno require		 Create table header	 Pre stuff	local team width Args teamwidth or 	local sort text 	local sort table val Args sortable table 	or no 	if yesno then sort text sortable end	local show played true	local hide played val Args hide played 	or no 	if yesno then show played false end		 Custom header	local group head text Args group header or 		table insert 			 Open table		 Initialize	local tt 	tt count 		 Up by one after every call	tt tab text t		 Actual text	 Actual headers	tt p sub colhead										 Position col	 Add group header	if full table and group col then		tt p sub colhead							 Group col	end	tt p sub colhead						 Team col	if show played then		tt p sub colhead			 Matches played col	end		if full table then		tt p sub colhead				 Win col		tt p sub colhead				 Loss col	end	tt p sub colhead			 Points col		if full table then		tt p sub colhead	 Sets won col		tt p sub colhead		 Sets lost col		tt p sub colhead		 Set ratio col		tt p sub colhead	 Setpoints won col		tt p sub colhead	 Setpoints lost col		tt p sub colhead	 Setpoint ratio col	end 	if full table then		tt count tt count 		table insert	end 	return ttend function pp row	 Build the inner parts of individual rows		 Sub module usage	local mm require	local yesno require		 Get custom default options for in table	local bwin points tonumber			or 	local swin points tonumber			or 	local sloss points tonumber			or 	local bloss points tonumber			or 	 Get some input	local bigwins tonumber		or 	local smallwins tonumber		or 	local biglosses tonumber	or 	local smallosses tonumber	or 	local setswon tonumber 		or 	local setslost tonumber		or 	local setpointswon tonumber 	or 	local setpointslost tonumber	or 	local s pts tonumber or 	local hth local Args hth team code ii 				or nil		 Then calculate some values	local matches bigwins smallwins biglosses smallosses	local points bwin points bigwins swin points smallwins sloss points smallosses bloss points biglosses s pts	local wins bigwins smallwins	local losses biglosses smallosses	 Some local vars		local hth string	local tt return p sub hth	hth string tt return str	hth id list tt return list	notes exist tt return notes exist 	 What to rank the teams by	local ranking style Args ranking style or wins 	local win fw win string pts fw pts string	ranking style string lower	if ranking style