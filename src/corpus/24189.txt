 Windows is an operating system by Microsoft released in Windows added many new features to the last Windows operating system which was Windows x Windows was the first Windows with the Start button Since then it has been used in all Windows operating systems Although Windows is thought of as a full operating system it works over MS DOS Windows is like a visual version of DOS This means that Windows can run both MS DOS programs and Windows ones but programs made just for Windows will not work in DOS Windows was Microsoft s first operating system to support long file names Before that file names could only be eight characters long plus the file extension Windows was also Microsoft s first operating system to fully support CD ROM s though Windows x could also access CD ROMs It was popular from until when Microsoft replaced it with Windows 