In computer networking Teredo is a tunneling protocol used to give IPv connectivity to a computer with only an IPv connection The IPv address block is used with Teredo Unlike similar protocols such as Protocol Teredo works correctly even if the IPv network has a Network address translation network address translator Teredo is available on Windows XP and later and is enabled by default on Windows Vista and later It is also available on Linux and Unix with a program called Miredo Teredo was named after Teredo navalis commonly called a shipworm which creates holes or tunnels in wooden ships A Teredo address might look like e bf fff fdd This address has the Teredo server the client IPv address and the UDP port number tech stub 