The Template Fake image shows a box which looks like an empty image containing the strike through name as parameter under the empty image The top and inner margins each default to px but can be reset by style xxxx Parameters Note that for the style parameter there are no quotation mark s around the style settings even though in CSS style sheets the quotation marks would need to be used The format of parameters is fake image Only the image name is required This template is useful for delinking images in archives used on commons 